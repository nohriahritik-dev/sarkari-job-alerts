{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @workspace/job-alert run build",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
